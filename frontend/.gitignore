node_modules
# Keep environment variables out of version control
.env

/lib/generated/prisma

# Prevent blog content from being placed in pages directory
# (Next.js treats everything in pages/ as a page component)
/pages/blog/content/
