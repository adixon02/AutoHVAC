# API Configuration
# Local: http://localhost:8000
# Production: https://autohvac-backend.onrender.com
NEXT_PUBLIC_API_URL=http://localhost:8000

# NextAuth Configuration (REQUIRED IN PRODUCTION)
# Local: http://localhost:3000
# Production: https://autohvac.ai
NEXTAUTH_URL=http://localhost:3000
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl

# Database Configuration
# For Render: Use external URL (with .oregon-postgres.render.com)
# Internal URL (within Render): postgresql://user:pass@dpg-xxx-a/dbname
# External URL (outside Render): postgresql://user:pass@dpg-xxx-a.oregon-postgres.render.com/dbname
DATABASE_URL=postgresql://user:password@localhost:5432/autohvac

# Email Configuration for Magic Links (Optional)
EMAIL_SERVER_HOST=smtp.gmail.com
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your-email@example.com
EMAIL_SERVER_PASSWORD=your-app-password
EMAIL_FROM=AutoHVAC <noreply@example.com>

# Environment (set to 'production' in Render)
NODE_ENV=development